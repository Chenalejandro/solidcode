#include <algorithm>
#include <fstream>
#include <iostream>
#include <iterator>
#include <string>
#include <vector>

using namespace std;

long corteDeMayorGanancia(vector<long> precios, int longitud) {
  // Escribir el código acá
  return 0;
}
